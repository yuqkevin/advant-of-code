## Advent Of Code Solutions

### Start

* run `./start <dayname>` i.e `./start day11`
* Download your puzzle input into the day folder and save as `input.txt`
* Coding in <dayname>/solution.py 
* To get result, run:
  python  -m <dayname>.solution <part>
  For example: `python -m day9.solution part1`
